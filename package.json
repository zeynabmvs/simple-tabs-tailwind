{
  "name": "tabs_tailwind",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all --parallel sync tailwindwatch",
    "sync": "browser-sync start --server --files 'index.html' ",
    "tailwindwatch": "tailwindcss -i ./assets/css/style.css -o ./dist/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "-": "^0.0.1",
    "@tailwindcss/typography": "^0.5.9",
    "browser-sync": "^2.29.3",
    "npm-run-all": "^1.8.0",
    "tailwindcss": "^3.3.2"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.3"
  }
}
